<!  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
<section class="wrap">
<header class="gossip-header" center>
    <div>
    <h1 class="gossip-title mt-0">Le crousillant Profil de <%= User.find(params[:id]).first_name %></h1>
    </div>
    <span class="badge badge--fire">Profil</span>
</header>
<body>
    <div class="card card--sub tier-1 halo-fire"  >
        <dl class="mb-0 details-list">
        <dt class="muted">Nom &amp; prénom</dt>
        <dd class="mb-0"><%= User.find(params[:id]).first_name %> <%= User.find(params[:id]).last_name %></dd>
        <dt class="muted">Email</dt>
        <dd class="mb-0">
            <a href="mailto:<%= User.find(params[:id]).email %>"><%= User.find(params[:id]).email %></a>
        </dd>
        <dt class="muted">Âge</dt>
        <dd class="mb-0"><%= User.find(params[:id]).age %> ans</dd>
        <dt class="muted">Vile</dt>
        <dd class="mb-0"><%= User.find(params[:id]).city.name %></dd>
        <dt class="muted">Description</dt>
        <dd class="mb-0"><%= User.find(params[:id]).description %></dd>
        </dl>
    </div>
</body>

</section>

