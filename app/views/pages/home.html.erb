<section class="gp-card gp-fade-1">
  <div class="gp-card__header">
    <div>
      <h2>Bienvenue <%= User.find_by(id: session[:user_id]).first_name %> !</h2>
      <p class="gp-card-subtitle">Retrouve les scoops croustillants et notre équipe dédiée.</p>
    </div>
    <span class="gp-badge">🔥 Nouveaux ragots</span>
  </div>
  <div class="gp-flex gp-flex-between gp-gap-md">
    <p class="gp-text-muted gp-mb-0">
      Diffuse la bonne parole : partage un potin croustillant et découvre ceux de la communauté.
    </p>
    <%= link_to "Publier un nouveau gossip", new_gossip_path, class: "gp-btn gp-btn--primary" %>
  </div>
</section>

<section class="gp-card gp-fade-2">
  <div class="gp-card__header gp-mb-2">
    <h2 class="gp-card-title gp-mb-0">Les derniers potins</h2>
    <p class="gp-card-subtitle gp-mb-0">Un flot continu d’anecdotes aussi croustillantes qu’un croissant doré.</p>
  </div>

  <% if @gossips.present? %>
    <ul class="gp-list">
      <% @gossips.each do |gossip| %>
        <li class="gp-list-item">
          <div class="gp-meta">
            <% if gossip.user.present? %>
              <span class="gp-text-accent">
                <%= link_to gossip.user.first_name, user_path(gossip.user) %>
              </span>
            <% else %>
              <span class="gp-text-muted">Anonyme</span>
            <% end %>
            <span>•</span>
            <span class="gp-mono">##<%= gossip.id %></span>
          </div>
          <h3 class="gp-mb-1">
            <%= link_to gossip.title, gossip_path(gossip), class: "gp-text-accent" %>
          </h3>
          <p class="gp-mb-0"><%= truncate(gossip.content, length: 140) %></p>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="gp-text-muted gp-mb-0">Aucun potin pour le moment. Et si tu lançais la première rumeur ?</p>
  <% end %>
</section>

<section class="gp-card gp-neon-section gp-fade-3">
  <div class="gp-card-body">
    <p class="gp-mb-2">
      Explore les dernières rumeurs partagées par la communauté, et contacte-nous si tu veux participer à l’aventure.
    </p>
    <div class="gp-flex gp-gap-md">
      <%= link_to "Voir l'équipe", pages_team_path, class: "gp-btn gp-btn--primary" %>
      <%= link_to "Nous contacter", contact_path, class: "gp-btn gp-btn--ghost" %>
    </div>
  </div>
</section>
